#include <iostream>
#include<stack>
using namespace std;

int main() {
  stack<int>s;
  
  string st = "harsh" ;
  for(int i=0;i<st.size();i++){
    char ch = st[i];
    s.push(ch);

  }

  while(!s.empty()){
    cout<<s.top()<<endl;
    s.pop();
  }
  return 0;
}
